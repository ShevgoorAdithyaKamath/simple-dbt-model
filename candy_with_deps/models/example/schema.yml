
version: 2

models:
  - name: model3
    description: "Model that has dependecy on another project"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
